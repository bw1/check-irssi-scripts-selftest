sudo: false
dist: trusty
language: perl
cache:
  directories:
  - extlib
perl:
- "5.30"
addons:
  apt:
    packages:
    - irssi
    - libyaml-perl
    - libhtml-parser-perl
    - libjson-pp-perl
before_install:
- git clone -q -b chansearch2 https://github.com/bw1/scripts.irssi.org.git
install:
- 'echo $PWD'
before_script:
- 'tree ~'
script:
- perl runselfcheck.pl
after_script:
- cat tmp/info.yaml
branches:
  only:
  - master

